.env
*.log

# Regenerable from a live VM via scripts/mspc_build_payload.ps1; it ships inside the published image, not through git.
image/mspc/*.tar.gz
image/vda/*.tar.gz
# Chrome Enterprise offline MSI (159 MB): re-download from
# https://dl.google.com/edgedl/chrome/install/GoogleChromeStandaloneEnterprise64.msi
# (payload hash marker 1c555a95c691 = v152.0.7977.83); it ships in the image, not git.
image/chrome/*.msi
__pycache__/
